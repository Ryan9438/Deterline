#!/bin/bash
# 一键安装 undertale-save-editor 到本地 Agent
# 此脚本位于 Install.app/Contents/MacOS/install
# 项目根目录在 ../../../
cd "$(cd "$(dirname "$0")/../../.." && pwd)" || exit 1
python3 install.py
echo ""
echo "按回车键退出..."
read -r